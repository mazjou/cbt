<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <style>
    *{margin:0;padding:0;box-sizing:border-box;}
    body{font-family:Arial,sans-serif;background:#d1d5db;}

    /* ── Kontrol bar ─────────────────────────────────────────────────── */
    .no-print{background:white;padding:12px 20px;
      box-shadow:0 2px 8px rgba(0,0,0,.12);margin-bottom:14px;}
    .no-print-top{display:flex;align-items:center;justify-content:space-between;
      flex-wrap:wrap;gap:10px;margin-bottom:10px;}
    .no-print h2{font-size:15px;font-weight:700;color:#1e293b;}
    .ctrl{display:flex;gap:8px;align-items:center;}
    .btn{padding:8px 18px;border-radius:8px;font-weight:700;font-size:13px;
      cursor:pointer;border:none;text-decoration:none;display:inline-flex;align-items:center;gap:6px;}
    .btn-print{background:#4f46e5;color:white;}
    .btn-back{background:#f1f5f9;color:#475569;}

    /* Form pengaturan teks header */
    .settings{
      background:#f8fafc;border:1px solid #e2e8f0;border-radius:10px;
      padding:12px 16px;display:grid;grid-template-columns:repeat(3,1fr);gap:10px;
    }
    .settings label{font-size:11px;font-weight:600;color:#475569;display:block;margin-bottom:3px;}
    .settings input{width:100%;border:1.5px solid #cbd5e1;border-radius:6px;
      padding:6px 10px;font-size:12px;color:#1e293b;outline:none;}
    .settings input:focus{border-color:#6366f1;}
    .settings-note{font-size:10px;color:#94a3b8;margin-top:6px;}

    /* ── Halaman A4 — 4 kartu (2×2) ────────────────────────────────── */
    .page{
      width:210mm;height:297mm;
      margin:0 auto 16px;
      background:#d1d5db;
      padding:10mm;
      display:grid;
      grid-template-columns:80mm 80mm;
      grid-template-rows:105mm 105mm;
      gap:10mm;
      justify-content:center;
      overflow:hidden;
    }

    /* ── Kartu fixed 80×105mm ───────────────────────────────────────── */
    .kartu{
      width:80mm;height:105mm;
      border-radius:5mm;overflow:hidden;
      display:flex;flex-direction:column;
      background:#2563eb;
      box-shadow:0 3px 12px rgba(0,0,0,.3);
    }

    /* Header biru: 35mm */
    .k-header{
      height:35mm;flex-shrink:0;
      background:linear-gradient(160deg,#1d4ed8 0%,#2563eb 60%,#3b82f6 100%);
      display:flex;flex-direction:column;
      align-items:center;justify-content:center;
      padding:0 5mm;text-align:center;gap:1.5mm;
    }
    .k-hole{width:8mm;height:3mm;background:rgba(255,255,255,.3);
      border-radius:2mm;margin-bottom:1mm;}
    .k-line1{font-size:13pt;font-weight:900;text-transform:uppercase;
      color:white;letter-spacing:.5px;line-height:1.2;}
    .k-line2{font-size:6.5pt;font-weight:700;text-transform:uppercase;
      color:#bfdbfe;letter-spacing:.3px;line-height:1.4;}
    .k-line3{font-size:6pt;color:#93c5fd;line-height:1.4;}

    /* Badge jabatan kuning-emas: 10mm */
    .k-badge{
      height:10mm;flex-shrink:0;
      margin:0 4mm;
      background:linear-gradient(90deg,#d97706,#f59e0b,#fbbf24);
      border-radius:2mm;
      display:flex;align-items:center;justify-content:center;
      font-size:11pt;font-weight:900;text-transform:uppercase;
      color:white;letter-spacing:2px;
      text-shadow:0 1px 2px rgba(0,0,0,.25);
    }

    /* Body putih: flex sisa */
    .k-body{
      flex:1;
      background:white;
      margin:2mm 4mm 0;
      border-radius:2mm 2mm 0 0;
      display:flex;flex-direction:column;
      align-items:center;
      padding:3mm 4mm 2mm;
      gap:2mm;
      overflow:hidden;
    }

    /* Foto: 22×27mm */
    .k-foto{
      width:22mm;height:27mm;flex-shrink:0;
      border:1.5px solid #374151;
      border-radius:1mm;
      background:linear-gradient(135deg,#eff6ff,#dbeafe);
      display:flex;align-items:center;justify-content:center;
      overflow:hidden;
    }
    .k-foto img{width:100%;height:100%;object-fit:cover;}
    .k-foto-ph{display:flex;flex-direction:column;align-items:center;gap:.5mm;}
    .k-foto-ph svg{width:10mm;height:10mm;opacity:.35;}
    .k-foto-ph span{font-size:4pt;color:#94a3b8;font-weight:700;text-transform:uppercase;}

    /* Nama + NIP centered, tidak ada label */
    .k-nama{
      font-size:8.5pt;font-weight:900;color:#1e3a8a;
      text-align:center;line-height:1.3;
      border-bottom:1px solid #374151;
      width:100%;padding-bottom:1.5mm;
    }
    .k-nip{
      font-size:6.5pt;color:#475569;
      font-family:'Courier New',monospace;
      text-align:center;
      width:100%;
    }

    /* Ruang opsional */
    .k-ruang{
      font-size:7pt;font-weight:700;color:#1d4ed8;
      text-align:center;width:100%;
    }

    /* Footer biru: 14mm */
    .k-footer{
      height:14mm;flex-shrink:0;
      background:linear-gradient(90deg,#1d4ed8,#2563eb);
      margin:0 4mm;
      border-radius:0 0 2mm 2mm;
      display:flex;flex-direction:column;
      align-items:center;justify-content:center;gap:1mm;
    }
    .k-footer-name{font-size:7pt;font-weight:900;text-transform:uppercase;
      color:white;letter-spacing:.5px;}
    .k-footer-sub{font-size:5pt;color:#bfdbfe;}

    @media print{
      body{background:white;}
      .no-print{display:none!important;}
      .page{
        margin:0;width:210mm;height:297mm;
        padding:10mm;background:white;
        page-break-after:always;overflow:hidden;
      }
      .page:last-child{page-break-after:auto;}
      .kartu{box-shadow:none;}
      @page{size:A4 portrait;margin:0;}
    }
  </style>
</head>
<body>

<!-- Kontrol bar dengan pengaturan teks header -->
<div class="no-print">
  <div class="no-print-top">
    <div>
      <h2>🎫 Cetak ID Card Panitia Ujian</h2>
      <small style="color:#64748b"><%= panitia.length %> panitia &nbsp;·&nbsp; ID Card B3 (80×105mm) &nbsp;·&nbsp; 4 kartu/halaman</small>
    </div>
    <div class="ctrl">
      <button onclick="applySettings(); window.print();" class="btn btn-print">🖨️ Cetak / Save PDF</button>
      <a href="/admin/panitia" class="btn btn-back">← Kembali</a>
    </div>
  </div>

  <!-- Form isian header kartu — bisa diedit bebas -->
  <div class="settings">
    <div>
      <label>Baris 1 — Judul Besar</label>
      <input id="set-line1" value="<%= schoolInfo.name %>" placeholder="Nama sekolah / judul ujian" />
    </div>
    <div>
      <label>Baris 2 — Subjudul</label>
      <input id="set-line2" value="<%= schoolInfo.address %>" placeholder="Kota / instansi" />
    </div>
    <div>
      <label>Baris 3 — Tahun / Keterangan</label>
      <%
        var y = new Date().getFullYear();
        var ta = new Date().getMonth() >= 6 ? (y+'/'+(y+1)) : ((y-1)+'/'+y);
      %>
      <input id="set-line3" value="Tahun Pelajaran <%= ta %>" placeholder="TP / keterangan" />
    </div>
  </div>
  <div class="settings-note">💡 Ubah teks di atas lalu klik Cetak — perubahan langsung diterapkan ke semua kartu</div>
</div>

<%
  var schoolName = schoolInfo.name || 'SMK Negeri 1 Kras';
  var schoolAddr = schoolInfo.address || 'Kediri, Jawa Timur';
  var PH = 4;
%>

<% for(var p = 0; p < panitia.length; p += PH){ %>
<div class="page">
  <% for(var j = p; j < Math.min(p+PH, panitia.length); j++){
       var pn  = panitia[j];
       var foto = pn.profile_photo && pn.profile_photo.startsWith('/public/') ? pn.profile_photo : null;
  %>
  <div class="kartu">

    <!-- Header -->
    <div class="k-header">
      <div class="k-hole"></div>
      <div class="k-line1 hdr-line1"><%=schoolName%></div>
      <div class="k-line2 hdr-line2"><%=schoolAddr%></div>
      <div class="k-line3 hdr-line3">Tahun Pelajaran <%=ta%></div>
    </div>

    <!-- Badge jabatan kuning -->
    <div class="k-badge"><%=pn.jabatan%></div>

    <!-- Body putih -->
    <div class="k-body">
      <div class="k-foto">
        <% if(foto){ %>
          <img src="<%=foto%>" alt=""
            onerror="this.parentNode.innerHTML='<div class=\'k-foto-ph\'><svg viewBox=\'0 0 24 24\' fill=\'none\' stroke=\'#94a3b8\'><path stroke-linecap=\'round\' stroke-linejoin=\'round\' stroke-width=\'1.5\' d=\'M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z\'/></svg><span>FOTO</span></div>'">
        <% } else { %>
          <div class="k-foto-ph">
            <svg viewBox="0 0 24 24" fill="none" stroke="#94a3b8">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
            </svg>
            <span>FOTO</span>
          </div>
        <% } %>
      </div>

      <!-- Nama bold centered -->
      <div class="k-nama"><%=pn.full_name%></div>

      <!-- NIP di bawah nama, centered, dari data username guru -->
      <div class="k-nip"><%=pn.username%></div>

      <!-- Nomor ruang jika ada -->
      <% if(pn.nomor_ruang){ %>
        <div class="k-ruang">Ruang : <%=pn.nomor_ruang%></div>
      <% } %>
    </div>

    <!-- Footer biru -->
    <div class="k-footer">
      <div class="k-footer-name ftr-name"><%=schoolName%></div>
      <div class="k-footer-sub ftr-sub">TP <%=ta%></div>
    </div>

  </div>
  <% } %>
</div>
<% } %>

<script>
// Terapkan perubahan teks header ke semua kartu sebelum cetak
function applySettings() {
  var l1 = document.getElementById('set-line1').value;
  var l2 = document.getElementById('set-line2').value;
  var l3 = document.getElementById('set-line3').value;
  document.querySelectorAll('.hdr-line1').forEach(el => el.textContent = l1);
  document.querySelectorAll('.hdr-line2').forEach(el => el.textContent = l2);
  document.querySelectorAll('.hdr-line3').forEach(el => el.textContent = l3);
  document.querySelectorAll('.ftr-name').forEach(el => el.textContent = l1);
}
// Auto-apply saat input berubah (live preview)
['set-line1','set-line2','set-line3'].forEach(id => {
  document.getElementById(id).addEventListener('input', applySettings);
});
</script>
</body>
</html>
